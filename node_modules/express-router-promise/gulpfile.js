const gulp = require('gulp');
const babel = require('gulp-babel');

gulp.task(`default`, () => {
    const destination = gulp.dest('dist');
    const babelObject = babel();

    gulp.src(`src/**/*.js`)
        .pipe(babelObject)
        .pipe(destination)

});
